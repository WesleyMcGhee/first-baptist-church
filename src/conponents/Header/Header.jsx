import "./Header.css";
import logo from "../../photos/fbclogo.PNG";

const mapThroughLinks = (arr) => {
  arr.map((key) => {});
};

const Header = (props) => {
  return (
    <header>
      <img src={logo} />
    </header>
  );
};
